{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🚀 dev",
      "command": "pnpm",
      "args": ["start:dev"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    },
    {
      "label": "🧶 lint",
      "command": "pnpm",
      "args": ["lint"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
    {
      "label": "✨ Format",
      "command": "pnpm",
      "args": ["format"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    },
    {
      "label": "✅ Typecheck",
      "command": "pnpm",
      "args": ["typecheck"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    }
  ]
}
